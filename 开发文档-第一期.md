# 统一媒体资产管理中心 (Unified Media CMS)

**第一期 (V1.0) 功能需求与技术规格说明书**

## 1. 阶段目标与工程边界

**核心目标**：完成系统的底层基石搭建，并完整跑通 `BOOK`（如纯文本 TXT / 标准 EPUB）从自动嗅探、AI 提取、刮削仲裁到转换归档的全生命周期。 **架构共识**：

- 坚决分离“逻辑资产 (Asset)”与“物理文件 (File)”。
- 坚决解耦“存储厂商差异”，引入“存储节点 (Storage Node)”。
- 坚决抛弃“死代码流水线”，采用类似 Java Stream 的上下文驱动 (Context-Driven) 积木式节点引擎。
- 核心业务层与节点以 Java 原生实现为主，为后续的 gRPC 跨语言插件打好接口基础。

------

## 2. 数据库基石设计 (PostgreSQL DDL)

*架构规范：严禁核心检索维度进入 JSONB；多云存储配置下沉至 Storage Node。*

SQL

```
-- 1. 基础设施：存储节点表
CREATE TABLE storage_nodes (
    id UUID PRIMARY KEY,
    name VARCHAR(100) NOT NULL,          -- 如 "本地 NAS 盘", "MinIO 图书桶"
    provider_type VARCHAR(50) NOT NULL,  -- 'LOCAL', 'MINIO', 'S3'
    connection_config JSONB NOT NULL,    -- 厂商特有参数 (base_path 或 endpoint/bucket)
    is_readonly BOOLEAN DEFAULT false,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. 核心检索维度表 (一等公民)
CREATE TABLE categories (id UUID PRIMARY KEY, name VARCHAR(50) UNIQUE NOT NULL);
CREATE TABLE tags (id UUID PRIMARY KEY, name VARCHAR(50) UNIQUE NOT NULL);
CREATE TABLE creators (id UUID PRIMARY KEY, name VARCHAR(100) UNIQUE NOT NULL);

-- 3. 逻辑资产与物理文件表 (1:N)
CREATE TABLE media_assets (
    id UUID PRIMARY KEY,
    library_id UUID NOT NULL,            -- 逻辑库区分
    title VARCHAR(255) NOT NULL,
    media_type VARCHAR(50) NOT NULL,     -- V1 强制为 'BOOK'
    publish_year INTEGER,
    cover_url TEXT,
    summary TEXT,
    status VARCHAR(50) NOT NULL,         -- 'PROCESSING', 'PENDING_MANUAL', 'COMPLETED'
    tech_specs JSONB DEFAULT '{}'::jsonb,-- 仅限跨界不敏感的技术参数
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE media_files (
    id UUID PRIMARY KEY,
    asset_id UUID REFERENCES media_assets(id) ON DELETE CASCADE,
    storage_node_id UUID REFERENCES storage_nodes(id) ON DELETE RESTRICT,
    file_format VARCHAR(50) NOT NULL,    -- 'TXT', 'EPUB'
    relative_path TEXT NOT NULL,         -- 不包含桶名或根路径的相对键值
    file_size BIGINT,
    is_primary BOOLEAN DEFAULT true,
    UNIQUE (storage_node_id, relative_path)
);

-- 维度关联表 (外键省略)
CREATE TABLE asset_categories (asset_id UUID, category_id UUID, PRIMARY KEY (asset_id, category_id));
CREATE TABLE asset_tags (asset_id UUID, tag_id UUID, PRIMARY KEY (asset_id, tag_id));
CREATE TABLE asset_creators (asset_id UUID, creator_id UUID, role VARCHAR(50), PRIMARY KEY (asset_id, creator_id, role));

-- 4. 状态机与留痕表
CREATE TABLE pipeline_tasks (
    id UUID PRIMARY KEY,
    asset_id UUID REFERENCES media_assets(id),
    current_status VARCHAR(50) NOT NULL, -- 'QUEUED', 'RUNNING', 'PENDING_MANUAL', 'FAILED'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE pipeline_task_logs (
    id UUID PRIMARY KEY,
    task_id UUID REFERENCES pipeline_tasks(id) ON DELETE CASCADE,
    node_name VARCHAR(100) NOT NULL,     
    status VARCHAR(50) NOT NULL,         -- 'SUCCESS', 'SKIPPED', 'FAILED'
    output_payload JSONB,                -- 节点的局部输出数据留痕
    error_message TEXT,
    execution_time_ms BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

------

## 3. 后端功能需求 (Spring Boot 调度与节点引擎)

### 3.1 积木式节点接口 (The Pipeline Node)

系统底层必须基于流式上下文 (`TaskContext`) 流转。

Java

```
public interface PipelineNode {
    String getNodeName();
    // 引擎调度前调用：判断上下文是否满足本节点前置条件
    boolean canExecute(TaskContext context); 
    // 核心执行逻辑，直接修改 context，若需仲裁则抛出 ArbitrationRequiredException
    void execute(TaskContext context) throws Exception;
}
```

### 3.2 第一期内置 Java 节点池

开发需实现以下原子节点（它们像积木一样互不干扰）：

1. **`FileSnifferNode`**: 探测输入文件的真实 MIME 类型并写入 Context。
2. **`TxtExtractorNode`**: (前置要求：MIME为text/plain) 提取前置 3000 字。
3. **`EpubMetaParserNode`**: (前置要求：MIME为epub) 解析内部 `content.opf`，提取已有元数据。
4. **`LlmAnalyzerNode`**: (前置要求：已有提取文本) 调用 Spring AI (Ollama/OpenAI) 提取 `{"title": "", "author": "", "tags": []}`。
5. **`DoubanScraperNode`**: (前置要求：已有 title) 跨网刮削。若结果不唯一，抛出需仲裁异常挂起任务。
6. **`FormatConverterNode`**: (可选配置节点) 将解析完善的 TXT 转化为标准的 EPUB 内存流。
7. **`ArchiveNode`**: 流程终点。根据配置，将处理好的文件物理写入指定的 `Storage Node`，并更新全部数据库表，标记状态为 `COMPLETED`。

### 3.3 动态流水线装配 (Pipeline Orchestrator)

系统不再硬编码执行顺序。管理员通过 JSON 定义“处理模板 (Profile)”。 引擎执行时：

1. 载入模板 JSON 定义的 Node 数组。
2. 创建 `TaskContext`。
3. `for (Node node : nodes)` 循环执行。遇到 `canExecute == false` 则打日志跳过。
4. 每执行完一个节点，强制向 `pipeline_task_logs` 异步写入一条带 JSON 留痕的记录。

------

## 4. 前端交互功能需求 (Vue 3 UI)

### 4.1 全局设置模块 (Settings)

- **存储节点管理 (Storage Nodes)**：
  - 提供表单新建本地挂载点（填写 Base Path）或 MinIO 挂载点（填写 Endpoint, Bucket, AK, SK）。
- **AI 算力配置 (LLM Config)**：
  - 配置全局大模型的 Base URL、API Key 与 Model Name。

### 4.2 生产车间 (Workshop View)

- **全局流水线看板**：列表展示所有排队、运行、异常的任务。
- **节点溯源抽屉**：点击任务行，右侧划出抽屉，以时间轴形式展示 `pipeline_task_logs`。开发人员或极客可点击查看每一步（如 AI 提取阶段）产生的真实 JSON 数据。
- **人工仲裁工作台 (Inbox Modal)**：
  - 筛选出 `PENDING_MANUAL` 状态的任务。
  - 点击后，左半屏展示流水线传递过来的残缺上下文（如 AI 猜出的书名），右半屏展示刮削节点返回的“多个候选结果”。
  - 用户选择正确结果，或手动编辑内容后点击“提交”。前端发送请求至 `/api/tasks/{taskId}/resume`，后端使用合并后的数据**唤起新线程**继续执行后续流。

### 4.3 消费媒体库 (Library View)

- **纯净海报墙**：主视图仅查询 `status='COMPLETED'` 的资产，支持按分类和标签过滤。
- **逻辑与物理折叠详情页 (Detail Page)**：
  - 点击卡片进入沉浸式视图，展示该“逻辑资产”的巨幅封面、简介、标签和作者。
  - **核心区**：页面下方列出 `media_files` 表中关联的所有物理文件实体（例如：`文件1：EPUB - 存在 MinIO 图书桶`，`文件2：TXT原始备份 - 存在 本地冷备盘`），并提供对应的独立下载/预览按钮。
  - **快编弹窗**：右上角提供“编辑”按钮，可手动修正元数据并即时存入 DB。

------

## 5. 核心 API 契约草案 (RESTful)

1. `GET /api/nodes`: 获取系统当前支持的所有积木节点及其所需配置 schema。
2. `POST /api/templates`: 保存一段由节点拼装的 JSON 流水线模板。
3. `POST /api/tasks/submit`: 提交一个新文件（附带其所在的 storage_node_id 和相对路径）触发状态机。
4. `POST /api/tasks/{taskId}/resume`: 仲裁通过，回传修正后的 Payload 重新激活任务。
5. `GET /api/assets/{id}`: 联表查询逻辑资产极其下挂的所有存储节点与物理文件详情。



PS：

https://github.com/herraristotle/BookLore
https://github.com/herraristotle/BookLore/blob/master/assets/booklore-screenshot.jpg
页面设计可以参考以上项目中的主页设计。