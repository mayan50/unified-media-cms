# 统一媒体资产管理中心 V1.0 — 功能清单

## 项目概览

基于 Spring Boot 3.3 + Vue 3 + Element Plus 构建的全栈媒体资产管理系统，采用 Monorepo 结构。

```
unified-media-cms/
├── backend/          # Spring Boot 后端
├── frontend/         # Vue 3 前端
└── sql/              # 数据库初始化脚本
```

---

## 一、后端功能 (Spring Boot)

### 1.1 积木式 Pipeline 节点引擎

系统核心引擎，基于 `TaskContext` 上下文在节点间流转，支持动态装配。

| 组件 | 文件 | 说明 |
|------|------|------|
| `PipelineNode` | pipeline/PipelineNode.java | 节点契约接口，定义 `getNodeName()`、`canExecute()`、`execute()` |
| `TaskContext` | pipeline/TaskContext.java | 流式上下文载体，携带文件信息、探测结果、AI 分析、刮削结果、最终元数据 |
| `ArbitrationRequiredException` | pipeline/ArbitrationRequiredException.java | 仲裁必需异常，挂起任务等待人工干预 |
| `PipelineOrchestrator` | service/PipelineOrchestrator.java | 流水线编排器，按模板动态加载节点、循环执行、异步写日志、支持仲裁恢复 |

**编排逻辑**：
1. 载入模板 JSON 定义的 Node 数组
2. 创建 `TaskContext`
3. 遍历节点，`canExecute == false` 则跳过并记录 SKIPPED 日志
4. 每个节点执行后，异步写入 `pipeline_task_logs` 留痕
5. 遇到 `ArbitrationRequiredException` 挂起任务为 `PENDING_MANUAL`
6. 仲裁恢复后从中断点继续执行

### 1.2 七个内置 Pipeline 节点

| 节点 | 前置条件 | 功能 |
|------|----------|------|
| `FileSnifferNode` | 有文件路径 | 探测文件真实 MIME 类型，识别格式（TXT/EPUB）写入 Context |
| `TxtExtractorNode` | MIME 为 text/plain | 提取 TXT 前 3000 字写入 Context |
| `EpubMetaParserNode` | MIME 为 epub | 解析 EPUB 内 content.opf，提取 title/creator/date/description |
| `LlmAnalyzerNode` | 已有提取文本 | 调用 Spring AI（Ollama / OpenAI）提取结构化元数据：title, author, tags, summary |
| `DoubanScraperNode` | 已有 title | HTTP 爬虫抓取豆瓣搜索结果；唯一结果自动采用，多个结果抛出仲裁异常 |
| `FormatConverterNode` | 格式为 TXT | 将 TXT 转换为标准 EPUB（mimetype + container.xml + content.opf + chapter1.xhtml + nav.xhtml） |
| `ArchiveNode` | 有标题 | 终点节点：更新资产元数据、处理标签/作者、写入转换后文件、标记 COMPLETED |

### 1.3 数据库层

**DDL 文件**: `sql/init.sql`，共 11 张表 + 7 个索引

| 表 | 说明 |
|----|------|
| `storage_nodes` | 存储节点（LOCAL/MINIO/S3），connection_config 为 JSONB |
| `categories` | 分类维度（一等公民，不进 JSONB） |
| `tags` | 标签维度 |
| `creators` | 创作者维度 |
| `media_assets` | 逻辑资产，关联 library_id、status、tech_specs(JSONB) |
| `media_files` | 物理文件（1:N 挂在 asset 下），关联 storage_node_id |
| `asset_categories` | 资产-分类关联 |
| `asset_tags` | 资产-标签关联 |
| `asset_creators` | 资产-创作者关联（含 role） |
| `pipeline_tasks` | 流水线任务（QUEUED/RUNNING/PENDING_MANUAL/FAILED） |
| `pipeline_task_logs` | 节点执行日志（SUCCESS/SKIPPED/FAILED + JSON 留痕） |
| `pipeline_templates` | 流水线模板（动态装配 JSON） |
| `app_settings` | 全局设置（key-value，value 为 JSONB） |

**JPA 实体**: 9 个实体类，对应上述核心表
**Repository**: 9 个 Spring Data JPA 接口，含自定义查询方法

### 1.4 REST API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/nodes` | 获取系统所有积木节点及配置 schema |
| GET | `/api/templates` | 列出所有流水线模板 |
| POST | `/api/templates` | 创建流水线模板（节点名称数组） |
| PUT | `/api/templates/{id}` | 更新模板 |
| DELETE | `/api/templates/{id}` | 删除模板 |
| POST | `/api/tasks/submit` | 提交新任务（storageNodeId + relativePath） |
| POST | `/api/tasks/{taskId}/resume` | 仲裁恢复，回传修正后的 Payload |
| GET | `/api/tasks` | 列出所有任务，支持 `?status=` 筛选 |
| GET | `/api/tasks/{taskId}` | 获取任务详情 |
| GET | `/api/tasks/{taskId}/logs` | 获取任务节点执行日志 |
| GET | `/api/assets` | 列出资产，支持 `?status=COMPLETED` 筛选 |
| GET | `/api/assets/{id}` | 联表查询资产详情 + 所有物理文件 + 存储节点信息 |
| PUT | `/api/assets/{id}` | 更新资产元数据 |
| GET | `/api/storage-nodes` | 列出存储节点 |
| POST | `/api/storage-nodes` | 创建存储节点 |
| PUT | `/api/storage-nodes/{id}` | 更新存储节点 |
| DELETE | `/api/storage-nodes/{id}` | 删除存储节点 |
| GET | `/api/settings/llm` | 获取 AI 配置 |
| POST | `/api/settings/llm` | 保存 AI 配置（provider/baseUrl/apiKey/modelName） |

### 1.5 其他后端功能

| 功能 | 说明 |
|------|------|
| CORS 跨域 | `WebConfig` 配置全局 CORS，允许前端开发服务器访问 |
| 异步执行 | `AsyncConfig` 配置线程池（core=4, max=8），Pipeline 异步执行 |
| 全局异常处理 | `GlobalExceptionHandler` 统一处理 IllegalArgumentException、Validation、通用异常 |
| Spring AI 双后端 | 同时支持 Ollama（本地）和 OpenAI 兼容 API，通过 `app.llm.provider` 配置切换 |

---

## 二、前端功能 (Vue 3 + Element Plus)

### 2.1 全局布局

- 顶部深色导航栏，包含 Logo + 三个主菜单（媒体库 / 生产车间 / 设置）
- Vue Router 路由管理，支持懒加载
- Pinia 状态管理（Asset Store + Task Store）
- Axios 封装，统一 `/api` 代理到后端 8080 端口

### 2.2 媒体库页面 (LibraryView)

| 功能 | 说明 |
|------|------|
| 海报墙展示 | 网格布局展示所有 `status=COMPLETED` 的资产，卡片含封面图 + 标题 + 类型 + 年份 |
| 搜索 | 支持按标题关键词实时搜索筛选 |
| 卡片交互 | hover 上浮动效，点击进入详情页 |
| 空状态 | 无数据时展示 Empty 组件 |
| 骨架屏 | 加载中显示 Skeleton 动画 |

### 2.3 资产详情页 (AssetDetailView)

| 功能 | 说明 |
|------|------|
| 沉浸式英雄区 | 左侧巨幅封面，右侧标题 + 年份标签 + 简介 |
| 物理文件列表 | Element Plus Table 展示所有关联的物理文件（格式/路径/存储节点/大小/主文件标记） |
| 下载按钮 | 每个物理文件行的下载操作按钮 |
| 快编弹窗 | 右上角"编辑"按钮，弹出 Dialog 可修改标题、出版年份、封面URL、简介，即时保存 |

### 2.4 生产车间页面 (WorkshopView)

| 功能 | 说明 |
|------|------|
| 全局流水线看板 | 表格展示所有任务（ID/资产ID/状态/创建时间），支持状态筛选 |
| 提交新任务 | 弹窗输入存储节点 ID + 文件路径，触发 Pipeline |
| 状态标签 | QUEUED(蓝) / RUNNING(橙) / COMPLETED(绿) / PENDING_MANUAL(红) / FAILED(红) |
| 节点溯源抽屉 | 点击任务行，右侧 Drawer 以 Timeline 展示所有节点日志，含执行状态、耗时、错误信息 |
| JSON 数据查看 | 每个日志节点可折叠展开查看原始 JSON outputPayload |
| 人工仲裁弹窗 | 左半屏展示 AI 建议，右半屏可编辑/选择最终结果，提交后调用 resume 接口继续执行 |

### 2.5 设置页面 (SettingsView)

| 功能 | 说明 |
|------|------|
| 存储节点管理 | 表格展示所有存储节点，支持新建和删除 |
| 新建存储节点 | 弹窗表单：选择类型（LOCAL/MINIO/S3），LOCAL 填 Base Path，MINIO/S3 填 Endpoint/Bucket/AK/SK |
| AI 算力配置 | 配置 LLM 提供商（Ollama / OpenAI）、Base URL、API Key、模型名称 |

---

## 三、技术栈

| 层 | 技术 | 版本 |
|----|------|------|
| 后端框架 | Spring Boot | 3.3.6 |
| ORM | Spring Data JPA | - |
| 数据库 | PostgreSQL | - |
| AI 框架 | Spring AI | 1.0.0-M4 |
| HTML 解析 | Jsoup | 1.18.1 |
| 工具库 | Lombok | - |
| 前端框架 | Vue 3 | 3.5.x |
| UI 框架 | Element Plus | 2.9.x |
| 状态管理 | Pinia | 2.2.x |
| 路由 | Vue Router | 4.4.x |
| HTTP | Axios | 1.7.x |
| 构建 | Vite | 8.x |
| 语言 | TypeScript | 6.x |

---

## 四、启动方式

### 1. 初始化数据库

```bash
psql -U postgres -c "CREATE DATABASE unified_media_cms;"
psql -U postgres -d unified_media_cms -f sql/init.sql
```

### 2. 启动后端

```bash
cd backend
# 修改 application.yml 中的数据库连接信息
mvn spring-boot:run
# 后端运行在 http://localhost:8080
```

### 3. 启动前端

```bash
cd frontend
npm install   # 首次需要安装依赖
npm run dev
# 前端运行在 http://localhost:5173，自动代理 /api 到后端
```
